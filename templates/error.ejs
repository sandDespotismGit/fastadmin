<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <style>
      body {
        font-family: "Montserrat", sans-serif;
        padding: 40px;
        background-color: #fff5f5;
      }
      .error-container {
        max-width: 600px;
        margin: 0 auto;
        border: 1px solid #f44336;
        background: #ffeaea;
        padding: 20px;
        border-radius: 10px;
      }
      h1 {
        color: #f44336;
      }
      a {
        color: #2196f3;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="error-container">
      <h1>⚠️ <%= title %></h1>
      <p><%= message %></p>
      <p><a href="<%= returnLink %>">⬅ Вернуться назад</a></p>
    </div>
  </body>
</html>
